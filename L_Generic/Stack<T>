using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StackT : MonoBehaviour
{
    void Start()
    {
        // Stack<T>
        Stack<int> stack = new Stack<int>(); // Stack의 일반화

        stack.Push(10);
        stack.Push(20);
        stack.Push(30);
        stack.Push(40);
        stack.Push(50);

        stack.Pop();

        foreach (int n in stack)
        {
            Debug.Log(n);
        }
    }
}
